"""Packaged starter run-configuration files (data only).

One TOML per registered recipe, pinned to the recipe's calibrated
values. Discover them with
:func:`courtside_dynamics.run_config.available_run_configs` and copy
one for editing with
:func:`courtside_dynamics.run_config.copy_starter_config`.
"""
